# ARGUS — Evidencia de eliminación (Prompt 20)

> Cada fila fue verificada independientemente antes de eliminar: grep de
> imports estáticos, grep de nombres de símbolo exportado (no solo nombre de
> archivo), y — para los casos con backlog — lectura de
> `ARGUS_MASTER_BACKLOG.md` y `ARGUS_SENAPRED_CANONICAL_INGESTION.md`. Todas
> las suites relevantes (`test:p0`, `test` completo) se ejecutaron después
> de cada lote y pasaron antes de continuar.

| Archivo o sistema | Búsquedas realizadas | Consumidores | Decisión | Test |
|---|---|---|---|---|
| `src/lib/ingest/deduplicationEngine.ts`, `eventNormalizer.ts`, `ingestJobRegistry.ts`, `ingestJobTypes.ts`, `retryPolicy.ts` | Grep de `lib/ingest/<nombre>` en `src/`, `scripts/`, `tests/` | Solo referencias internas entre los 5 archivos (ninguna externa) | `delete_confirmed` | `npm run test` completo (510→510 sin estos archivos) |
| `conafAdapter.ts`, `conasetAdapter.ts`, `csbAdapter.ts`, `desinventarAdapter.ts`, `emdatAdapter.ts`, `iaeaAdapter.ts`, `nhtsaAdapter.ts`, `ntsbAdapter.ts`, `senapredAdapter.ts` (9 stubs) | Grep por nombre de archivo en `src/`, `scripts/`, `tests/`; lectura de `ARGUS_MASTER_BACKLOG.md` §P3; lectura de `ARGUS_SENAPRED_CANONICAL_INGESTION.md` línea 18 (nota explícita sobre el stub SENAPRED) | Solo 1 referencia (comentario de texto en `sourceOperationsRegistry.ts`, no import); backlog P3#1 autoriza explícitamente "eliminar o implementar" | `delete_confirmed` (backlog autoriza eliminar; §8 prohíbe implementar en esta tarea) | `npm run test` completo |
| `hermesNexusBridge.ts`, `arcaNexusBridge.ts`, `auraNexusBridge.ts`, `fenixNexusBridge.ts` | Grep de nombre de archivo y de cada símbolo exportado en `src/`, `tests/` | Solo `tests/p0/nexus-stabilization.test.ts` (test que afirmaba "cero importador" — ahora afirma "cero archivo") | `delete_confirmed` | `tests/p0/nexus-stabilization.test.ts` actualizado y verde |
| `src/modules/fenix/components/FenixDashboard.tsx` | Grep de `FenixDashboard` en `src/`, `tests/` | Solo comentarios explicativos + `tests/p0/fenix-canonicalization.test.ts` (aserción actualizada de "existe" a "no existe") | `delete_confirmed` | `tests/p0/fenix-canonicalization.test.ts` verde (26 casos) |
| `persistCorrelations()` (`src/lib/ingestion/persistExternalEvents.ts`) | Grep del nombre de función en `src/`, `scripts/`, `tests/` | Cero llamadores (solo su propia declaración) | `delete_confirmed` (función, no archivo ni tabla) | `npm run test` completo |
| `upsertEmbeddingRecord()` (`src/lib/knowledge-intake/vector/knowledgeVectorStore.ts`) | Grep del nombre de función en `src/`, `scripts/`, `tests/` | Cero llamadores | `delete_confirmed` (función, no archivo ni tabla) | `npm run test` completo |
| `maplibre-gl` (`package.json`) | Grep de `maplibre` en `src/`, `scripts/`, configs | Cero referencias — stack real es Leaflet + Three.js | `delete_confirmed` | `npm install` reconcilia lockfile; `npm run build` no depende de él |
| 51 helpers de contexto GVP/IOC/NOAA/OpenAQ/tsunami/volcán (`src/lib/{aura,fenix,nav,risk,ocean,tsunami,volcano,wildfire,tropical-cyclone,command-center}/*Context.ts`) | Grep de nombre de archivo + cada símbolo exportado en `src/`, `tests/`; **lectura de contenido de 4 archivos muestra** para juzgar valor científico | Cero importadores, cero tests — PERO contienen lógica de dominio real y deliberada (no stubs) | **`quarantine`** (revertido de una clasificación preliminar `delete_confirmed` al inspeccionar contenido — ver `ARGUS_TECHNICAL_DEBT_CLOSURE.md`) | No aplica — no se tocaron |
| `src/lib/vigia/alertPromotionEngine.ts` → `globalAlertPromotionEngine.ts` | Grep de import en 6 archivos consumidores | `globalWatchEngine.ts`, `wildfireCorrelationEngine.ts` (ambos activos) | `consolidate` (renombrar, no eliminar) | `tests/vigia/wildfireCorrelationPolicy.test.ts`, `tests/vigia/wildfireCorrelationEngine.integration.test.ts` verdes |
| `src/lib/incidents/alertPromotionEngine.ts` → `chileAlertPromotionEngine.ts` | Ídem | `globalWatchEngine.ts`, `src/app/api/chile-alerts/run/route.ts` (ambos activos) | `consolidate` (renombrar, no eliminar) | `tests/senapred/senapredConsolidation.test.ts`, `tests/jobs/jobLockEndpoints.test.ts` verdes |
| `RouteHazardSeverity`, `HazardSeverity`, `WeatherSeverity` | Grep de cada nombre de tipo en `src/`, `tests/`; comparación de valores literales con `ArgusIngestionSeverity`/`NwsSeverity` | Cero consumidores externos del nombre de tipo; valores idénticos byte-a-byte | `consolidate` (alias `@deprecated`, no fusión de código) | `npx tsc --noEmit` limpio |
| 33 archivos `src/**/__tests__/*.test.ts` | Confirmado (vía `vitest.config.ts`) que Vitest solo incluye `tests/**`; grep de cada `runXTest`/función exportada — cero llamadores externos | Ninguno ejecuta el test; el código bajo prueba SÍ está activo | `consolidate` (mover + convertir formato, preservar cada aserción) | Los 33 nuevos archivos en `tests/` pasan (329 tests nuevos) |
| `EventSeverity`/`IncidentSeverity`/`ArgusSeverity`/`ArgusIncidentSeverity` | Comparación de valores literales y casing; conteo de consumidores por tipo | Amplios, independientes, semánticamente distintos | `defer_insufficient_evidence` (no son duplicados reales) | No aplica — no se tocaron |
| 6 rutas API huérfanas (`external-events`, `conflict-events`, `conflict-zones`, `command/sources`, `news-evidence`, `quakesense/clusters`) | Grep de cada ruta en `src/`, `tests/` | Cero consumidores encontrados en el repo | `defer_insufficient_evidence` (el prompt prohíbe eliminar API pública sin poder descartar consumo externo) | No aplica — no se tocaron |
| `ExternalEventCorrelation`, `KnowledgeEmbeddingRecord` (modelos Prisma) | Grep de lectores/escritores | Escritores eliminados (ver arriba); modelo en sí sin lectores activos | `defer_requires_migration` (tocar el modelo requeriría schema/migración, prohibido) | No aplica — cero cambios Prisma |
